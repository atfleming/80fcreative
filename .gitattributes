## Git LFS disabled for this repository [DM]
# We previously tracked many media files (jpg, png, gif, mp4, mov)
# via Git LFS which is no longer desired for deployment on Vercel
# due to exceeded LFS quotas. We now store these as regular Git blobs.
# If you need LFS again in the future, reintroduce patterns here.
